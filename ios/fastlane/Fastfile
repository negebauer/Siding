# Docs
#   https://docs.fastlane.tools
# Available actions
#   https://docs.fastlane.tools/actions

default_platform :ios

platform :ios do
  before_all do
    setup_circle_ci
  end

  lane :build do
    build_app
  end

  lane :build_ci do
    match type: 'appstore'
    build_app
  end
end
